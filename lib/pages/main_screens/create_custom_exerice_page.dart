import 'package:flutter/material.dart';

class CreateCustomExericePage extends StatelessWidget {
  const CreateCustomExericePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create Custom Exercise'),
      ),
    );
  }
}