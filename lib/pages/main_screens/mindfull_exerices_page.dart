import 'package:flutter/material.dart';

class MindfullExericePage extends StatelessWidget {
  const MindfullExericePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Mindfull Exercise'),
      ),
    );
  }
}